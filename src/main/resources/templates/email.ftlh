<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Next App</title>
    <meta name="description" content="Generated by create next app">
    <style>
        body {
            background-color: #F0FFF4; /* bg-green-50 */
            max-width: 100%;
            max-height: 100%;
            margin: 0;
            padding: 0;
        }
        .main-content {
            padding-top: 20px; /* pt-5 */
        }
        .logo-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 80px; /* pt-20 */
            padding-bottom: 28px; /* pb-7 */
        }
        .content-container {
            background-color: #ffffff; /* bg-white */
            border-radius: 4px; /* rounded-sm */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* shadow-lg */
            overflow: hidden;
            max-width: 36rem; /* max-w-xl */
            margin: 0 auto; /* mx-auto */
            justify-content: center;
            align-items: center;
        }
        .email-logo-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px; /* py-5 */
        }
        .email-logo {
            width: 100px; /* w-25 */
            height: 80px; /* h-20 */
            margin-top: 20px; /* mt-5 */
        }
        .text-container {
            padding-left: 44px; /* px-11 */
            padding-right: 44px; /* px-11 */
        }
        .email-title {
            font-weight: 600; /* font-semibold */
            font-size: 14px; /* text-sm */
            text-align: center; /* text-center */
            padding-bottom: 28px; /* pb-7 */
            color: #4A5568; /* text-gray-600 */
        }
        .greeting {
            font-size: 14px; /* text-sm */
            color: #4A5568; /* text-gray-600 */
            margin-bottom: 16px; /* mb-4 */
        }
        .email-body {
            font-size: 14px; /* text-sm */
            color: #2D3748; /* text-gray-700 */
            margin-bottom: 28px; /* mb-7 */
        }
        .button-container {
            display: flex;
            justify-content: start; /* justify-start */
            padding-bottom: 40px; /* pb-10 */
        }
        .accept-button {
            background-color: #1A4731; /* bg-green-900 */
            color: white;
            padding: 8px 28px; /* py-2 px-7 */
            border-radius: 4px; /* rounded */
            margin-right: 8px; /* mr-2 */
        }
        .decline-button {
            background-color: transparent;
            color: black;
            padding: 8px 28px; /* py-2 px-7 */
            border-radius: 4px; /* rounded */
            border: 1px solid #E2E8F0; /* border-gray-300 */
        }
        .footer-container {
            background-color: #ffffff; /* bg-white */
            border-radius: 4px; /* rounded-sm */
            display: flex;
            flex-direction: column; /* flex-col */
            gap: 32px; /* gap-8 */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* shadow-lg */
            overflow: hidden;
            max-width: 36rem; /* max-w-xl */
            margin: 0 auto; /* mx-auto */
            justify-content: center;
            align-items: center;
            margin-top: 4px; /* mt-1 */
            padding-left: 44px; /* px-11 */
            padding-right: 44px; /* px-11 */
            padding-top: 20px; /* py-5 */
            padding-bottom: 20px; /* py-5 */
        }
        .footer-content {
            display: flex;
            flex-direction: column; /* flex-col */
            gap: 20px; /* gap-5 */
            justify-content: center;
            align-items: center;
            width: 100%;
        }
        .team-name {
            font-size: 14px; /* text-sm */
            color: #4A5568; /* text-gray-600 */
        }
        .social-icons {
            display: flex;
            gap: 16px; /* gap-4 */
        }
        .footer-text {
            font-size: 13px; /* text-[13px] */
            color: #4A5568; /* text-gray-600 */
            display: flex;
            flex-direction: column; /* flex-col */
            justify-content: center;
            align-items: center;
            margin-bottom: 20px; /* mb-5 */
        }
    </style>
</head>
<body>

<table class="table-fixed" align="center" style="width: 100%; max-width: 600px; margin: 0 auto;">
    <tr>
        <td>
            <nav style="background-color: #1A4731; height: 76px;"></nav>
        </td>
    </tr>
    <tr>
        <td class="main-content">
            <table align="center" class="logo-container" style="width: 100%;">
                <tr>
                    <td align="center">
                        <img src="cid:logo.png" alt="logo" />
                    </td>
                </tr>
            </table>
            <table align="center" class="content-container" style="width: 100%;">
                <tr>
                    <td class="email-logo-container">
                        <img src="cid:email.png" alt="emailLogo" class="email-logo" />
                    </td>
                </tr>
                <tr>
                    <td class="text-container">
                        <h5 class="email-title">A new sign-in on Windows</h5>
                        <p id="account-owner" class="greeting">Hello Micheal,</p>
                        <p id="account-text" class="email-body">
                            Lorem ipsum dolor sit amet consectetur adipisicing elit. Quas esse numquam repudiandae atque sint modi cum voluptatibus qui quibusdam culpa.
                        </p>
                        <div class="button-container">
                            <button id="accept-button" class="accept-button">Accept</button>
                            <button id="decline-button" class="decline-button">Decline</button>
                        </div>
                    </td>
                </tr>
            </table>
        </td>
    </tr>
    <tr>
        <td>
            <footer>
                <table align="center" class="footer-container" style="width: 100%;">
                    <tr>
                        <td class="footer-content">
                            <p class="team-name">Karrabo Team</p>
                            <div class="social-icons">
                                <a href="img/twitter"><img src="cid:x.png" alt="twitter_handle" /></a>
                                <a href="img/facebook"><img src="cid:fb.png" alt="facebook" /></a>
                                <a href="img/instagram"><img src="cid:insta.png" alt="instagram" /></a>
                            </div>
                            <div class="footer-text">
                                <p>&copy; 2024 Karrabo Africa</p>
                                <p>312 Herbert Macaulay way, Sabo Yaba, Lagos</p>
                            </div>
                        </td>
                    </tr>
                </table>
            </footer>
        </td>
    </tr>
</table>

</body>
</html>
